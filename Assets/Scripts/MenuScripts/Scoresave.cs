using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Scoresave : MonoBehaviour
{
    public static int one = 0;
    public static int two = 0;
    public static int three = 0;
    public static int fourth = 0;
    public static int five = 0;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    { 
       /* if (one < Scorer.CurrentScore)
        {
            five = fourth;
            fourth = three;
            three = two;
            two = one;
            one = Scorer.CurrentScore;
        }
        else if (two < Scorer.CurrentScore)
        {
            five = fourth;
            fourth = three;
            three = two;
            two = Scorer.CurrentScore;
        }
        else if (three < Scorer.CurrentScore)
        {
            five = fourth;
            fourth = three;
            three = Scorer.CurrentScore;
        }
        else if (fourth < Scorer.CurrentScore)
        {
            five = fourth;
            fourth = Scorer.CurrentScore;
        }
        else if (five < Scorer.CurrentScore)
        {
            five = Scorer.CurrentScore;
        }
       */
    }
       
    }


  